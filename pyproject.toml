[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-payroll-system"
version = "0.1.0"
description = "Payroll domain and calculation components"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
